version: 0.2
phases:
  build:
    commands:
      - echo "Governance placeholder — replaced in later labs"
      - echo "Congrats on completing this first step!"
      - echo "You are ready for the next lab."
artifacts:
  files:
    - "**/*"
