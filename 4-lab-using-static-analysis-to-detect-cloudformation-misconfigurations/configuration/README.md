# Configuration Directory

This directory contains the CodeBuild definition for static analysis in lab 4.

## File Structure

```text
configuration/
└── buildspec.yml
```

## File Summaries

- `buildspec.yml`: Lab exercise buildspec template with placeholders for installing and running `cfn-lint`, plus an embedded spoiler/reference implementation.
